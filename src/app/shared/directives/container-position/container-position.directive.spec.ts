import { ContainerPositionDirective } from './container-position.directive';

describe('ContainerPositionDirective', () => {
  it('should create an instance', () => {
    const directive = new ContainerPositionDirective();
    expect(directive).toBeTruthy();
  });
});
